// // Import npm
const model = require('mongoose');

// // Import Helper
const responseHelper = require('../../helpers/response.helper');

// Name
const n = 'Search';

// // Search
exports.getSearch = async (result) => {
  try {
    let {  }
    return responseHelper.success(2, n, 200, )
  } catch (error) {
    
  }
}