const { Router } = require('express');
// // Import npm
const express = require('express');

// // Import Controller
const searchController = require('../../controllers/collections/search.controller');

// Create Router
const router = express.Router();

// Export
module.exports = router;